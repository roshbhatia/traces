# yaml-language-server: $schema=../../schema/provider.schema.json
version: provider/v1
name: git
description: Render a two-file diff with Git
command: [traces-provider-git]
requires:
  commands: [git]
defaults:
  timeout: 5s
actions:
  provider.validate:
    description: Check Git inside the provider runtime
    argv: [--validate]
  diff.render:
    description: Render a Git-compatible two-file diff
    argv: ["{{ .Local }}", "{{ .Remote }}", "{{ .Merged }}", "{{ .Color }}"]
    env:
      LOCAL: "{{ .Local }}"
      REMOTE: "{{ .Remote }}"
      MERGED: "{{ .Merged }}"
      TRACES_DIFF_COLOR: "{{ .Color }}"
      TRACES_DIFF_WIDTH: "{{ .Width }}"
